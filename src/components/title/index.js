import React from "react";
import {} from './styles.js';
const Title = ({children}) => {
    return (
        <div className="title">
            {children}
        </div>
    );
};

export default Title;